import React, { Component } from 'react';
/*
class PathwaySelectorItem extends React.Component {
  constructor(props) {
    super(props)
    this.state = {
      selected: props.selected
    }
    this.handleSelectionChange.bind(this);
  }

  handleSelectionChange(event) {

  }

  render() {
    return(
      /*
      <form>
        <label>
          {this.props.pathway.title}:
          <input 
            type="checkbox" checked={this.state.selected} onChange={this.handleSelectionChange}
          />
        </label>
      </form>
    );
    
  }
}


export default PathwaySelectorItem;
*/